import React from 'react';
import { StyleButton } from "./style";

const Button = () => {
    return(
            <StyleButton>
                Enviar
            </StyleButton>
    )
};

export default Button;